function productSum(array) {
  // Write your code here.
  console.log(parseInt("1+1+2"));
  for (let i = 0; i < array.length; i++) {
    if (typeof array[i] === "number") console.log("sd");
  }
}

productSum([5, 2, [7, -1], 3, [6, [-13, 8], 4]]);
